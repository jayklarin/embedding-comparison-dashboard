# 🧠 Embedding Comparison Dashboard

An interactive Streamlit app for exploring word relationships using GloVe embeddings, PCA projection, and cosine similarity.

## 🔧 Setup
```bash
pip install -r requirements.txt
streamlit run embedding-comparison-dashboard.py